# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Not committed on purpose: a Windows-generated package-lock.json breaks the
# Linux CI install (platform-specific optional deps → npm crash). CI resolves
# dependencies fresh from package.json instead.
package-lock.json

# Local working files & notes — keep OUT of the public repo (work-context, paths, config)
.claude/
CLAUDE.md

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
